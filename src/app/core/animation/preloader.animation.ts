import * as $ from 'jquery';
import { TweenLite, TimelineLite, Back } from 'gsap';

const init = () => {


};

const Preloader = {
  show: ({...opt}) => {
     // Pausar Animações ( se necessário )

    // Animar Logotipo ( true || false )

    // Nome da Seção a ser carregada

    // Routerlink da seção para carregar

    // Animar portas do preloader ( fechar )
  },
  hide: () => {
    // Animar portas do preloader ( abrir )

    // Executar Animações ( se necessário )

  }
};

export { Preloader };
